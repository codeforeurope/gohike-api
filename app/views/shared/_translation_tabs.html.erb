<% active ||= false %>
<li class="<%= "active" if active %>">
  <a href="#<%= translation_tab_id(resource, locale) %>" data-toggle="tab" data-locale="<%= locale %>">
    <% if locale != I18n.locale %>
      <button class="close close-tab" type="button">×</button>
    <% end %>
    <%= t(locale) %>

  </a>
</li>